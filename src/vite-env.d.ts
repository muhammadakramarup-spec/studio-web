/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` (ownership: F3, status/warden-log.md Decision W-2) from
// package.json's "version" field at build/dev/preview time.
declare const __APP_VERSION__: string;
