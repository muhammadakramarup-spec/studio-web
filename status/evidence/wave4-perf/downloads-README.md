# Download artifacts

The raw PNG/GLB/GLTF/Blender-ZIP/turntable-ZIP downloads captured by `measure.mjs` (three runs, about 26 MB) were removed from the repository for size. Their byte sizes and validation results are recorded in `measure-results.json` and `13-runtime-measurements.log`; re-run `measure.mjs` to regenerate them.
